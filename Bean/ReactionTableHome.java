package deefw;
// Generated 2015-12-29 13:42:13 by Hibernate Tools 4.3.1.Final

import java.util.List;
import javax.naming.InitialContext;
import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;
import org.hibernate.LockMode;
import org.hibernate.SessionFactory;
import static org.hibernate.criterion.Example.create;

/**
 * Home object for domain model class ReactionTable.
 * @see deefw.ReactionTable
 * @author Hibernate Tools
 */
public class ReactionTableHome
{

	private static final Log log = LogFactory.getLog(ReactionTableHome.class);

	private final SessionFactory sessionFactory = getSessionFactory();

	protected SessionFactory getSessionFactory()
	{
		try
		{
			return (SessionFactory) new InitialContext().lookup("SessionFactory");
		} catch (Exception e)
		{
			log.error("Could not locate SessionFactory in JNDI", e);
			throw new IllegalStateException("Could not locate SessionFactory in JNDI");
		}
	}

	public void persist(ReactionTable transientInstance)
	{
		log.debug("persisting ReactionTable instance");
		try
		{
			sessionFactory.getCurrentSession().persist(transientInstance);
			log.debug("persist successful");
		} catch (RuntimeException re)
		{
			log.error("persist failed", re);
			throw re;
		}
	}

	public void attachDirty(ReactionTable instance)
	{
		log.debug("attaching dirty ReactionTable instance");
		try
		{
			sessionFactory.getCurrentSession().saveOrUpdate(instance);
			log.debug("attach successful");
		} catch (RuntimeException re)
		{
			log.error("attach failed", re);
			throw re;
		}
	}

	public void attachClean(ReactionTable instance)
	{
		log.debug("attaching clean ReactionTable instance");
		try
		{
			sessionFactory.getCurrentSession().lock(instance, LockMode.NONE);
			log.debug("attach successful");
		} catch (RuntimeException re)
		{
			log.error("attach failed", re);
			throw re;
		}
	}

	public void delete(ReactionTable persistentInstance)
	{
		log.debug("deleting ReactionTable instance");
		try
		{
			sessionFactory.getCurrentSession().delete(persistentInstance);
			log.debug("delete successful");
		} catch (RuntimeException re)
		{
			log.error("delete failed", re);
			throw re;
		}
	}

	public ReactionTable merge(ReactionTable detachedInstance)
	{
		log.debug("merging ReactionTable instance");
		try
		{
			ReactionTable result = (ReactionTable) sessionFactory.getCurrentSession().merge(detachedInstance);
			log.debug("merge successful");
			return result;
		} catch (RuntimeException re)
		{
			log.error("merge failed", re);
			throw re;
		}
	}

	public ReactionTable findById(deefw.ReactionTableId id)
	{
		log.debug("getting ReactionTable instance with id: " + id);
		try
		{
			ReactionTable instance = (ReactionTable) sessionFactory.getCurrentSession().get("deefw.ReactionTable", id);
			if (instance == null)
			{
				log.debug("get successful, no instance found");
			} else
			{
				log.debug("get successful, instance found");
			}
			return instance;
		} catch (RuntimeException re)
		{
			log.error("get failed", re);
			throw re;
		}
	}

	public List<ReactionTable> findByExample(ReactionTable instance)
	{
		log.debug("finding ReactionTable instance by example");
		try
		{
			List<ReactionTable> results = (List<ReactionTable>) sessionFactory.getCurrentSession()
					.createCriteria("deefw.ReactionTable").add(create(instance)).list();
			log.debug("find by example successful, result size: " + results.size());
			return results;
		} catch (RuntimeException re)
		{
			log.error("find by example failed", re);
			throw re;
		}
	}
}
